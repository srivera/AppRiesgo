<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_search"
        android:icon="@drawable/llamar911"
        android:title="."
        android:showAsAction="always" />

</menu>